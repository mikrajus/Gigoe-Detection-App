name: gigoe_detection_app
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.3 <4.0.0"

dependencies:
  win32: ^5.5.0
  carousel_slider: ^5.1.1
  #cloud_firestore: ^4.9.0
  cloud_firestore: ^6.3.0
  connectivity_plus: ^7.1.1
  cupertino_icons: ^1.0.2
  dartz: ^0.10.1
  date_time_picker: ^2.1.0
  dio: ^5.1.2
  equatable: ^2.0.5
  #esys_flutter_share_plus: ^2.2.0
  share_plus: ^12.0.2
  #firebase_auth: ^4.8.0
  firebase_auth: ^6.4.0
  # firebase_core: ^2.15.1
  firebase_core: ^4.7.0
  #firebase_database: ^10.2.5
  firebase_database: ^12.3.0
  #firebase_storage: ^11.2.6
  firebase_storage: ^13.3.0
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  get: ^4.6.5
  get_it: ^9.2.1
  google_fonts: ^8.0.2
  image_picker: ^1.0.4
  path_provider: ^2.1.0
  provider: ^6.0.5
  qr_flutter: ^4.1.0
  tflite_flutter: ^0.12.1
  image: ^4.8.0


dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/icons/icon_profile.png
    - assets/images/appbar_logo.png
    - assets/images/logo.png
    - assets/images/logo2.png
    - assets/images/slider1.png
    - assets/images/slider2.png
    - assets/images/diagram.png
    - assets/images/slider3.png
    - assets/images/gigi_depan.JPG
    - assets/images/gigi_kanan.JPG
    - assets/images/gigi_kiri.JPG
    - assets/images/gigi_atas.JPG
    - assets/images/gigi_bawah.JPG
    - assets/images/dental.png
    - assets/images/doctor_register.png
    - assets/images/doctor_login.png
    - assets/images/statistik.png
    - assets/models/
  # An image asset can refer toeeee en one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
